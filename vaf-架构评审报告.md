# VAF 架构评审报告

> 评审对象：[vaf-调研与架构规划.md](./vaf-调研与架构规划.md)
>
> 评审日期：2026-08-05
>
> 评审范围：架构规划与项目入口文档；不包含可运行代码、部署配置或云环境，因此未对实现安全性和运行行为作验证。

## 1. 结论

**总分：70 / 100（B-，条件通过）**

这是一份方向正确、覆盖面完整的“软件研发控制平面”蓝图。它没有停留在“多 Agent 写代码”的层面，而是把规格、阶段门、追踪、独立验证和发布治理纳入了同一条链路；这正是 VAF 应有的核心价值。

不过，它现在更像产品/架构愿景，而不是可直接开工的实现规格。进入 M0 前，必须先补齐 3 个 P0：收窄 MVP、定义可恢复的状态机与副作用模型、把安全规则改成可执行策略。否则实现很容易变成一套文档生成器，或在后期才发现工作流恢复、权限隔离和发布治理无法落地。

**评审决定：允许进入“架构细化与 M0 设计”；不建议直接进入完整代码流水线或生产部署实现。**

## 2. 评分标准与结果

| 维度 | 权重 | 得分 | 评语 |
|---|---:|---:|---|
| 问题定位与产品边界 | 10 | 8 | 控制平面定位准确，但“企业级全流程”尚未收敛为首个可验证用户场景。 |
| 模块划分与依赖方向 | 15 | 14 | Orchestrator、Artifact、Policy、Sandbox、CI/CD 的职责划分清晰，且 LangGraph 不侵入领域层的取舍正确。 |
| 工作流、阶段门与追踪 | 20 | 14 | 阶段产物和审批职责完整；状态转移、追踪数据模型和覆盖率定义不足。 |
| 安全与发布治理 | 20 | 13 | 默认人工生产审批、OIDC、worktree 和不可变制品方向正确；策略执行点、供应链与审计边界未定义。 |
| 可靠性与恢复能力 | 15 | 9 | 已意识到暂停/恢复/重试，但尚未给出幂等、副作用、锁和补偿设计。 |
| MVP 可交付性与演进节奏 | 10 | 7 | A → B → C 的技术演进合理；当前 MVP 把文档、代码、回归、staging、生产发布同时纳入，范围过宽。 |
| 可观测性、评测与运营 | 10 | 5 | 已提出 Evidence、成本、规则沉淀和对比评测；但没有可验证指标。 |
| **合计** | **100** | **70** | **可作为设计方向，需先完成 P0 细化。** |

## 3. 已做得好的部分

1. **定位没有走偏。** 规划将 VAF 定义为“规格为事实来源、阶段门为质量约束、CI/CD 为交付出口”的控制平面（规划第 13–28 行），避免了把项目做成单纯 Prompt 集或聊天式代码生成器。

2. **架构分层合理。** 组件表将工作流编排、产物存储、规则、代码工作区、沙箱、模型适配、CI/CD 与证据链拆开（第 182–193 行）。特别是把 LangGraph 限定为状态与节点编排、将 Prompt/权限/模板放入 VAF 自己领域层（第 199–203 行），有利于以后更换运行时。

3. **流程闭环意识强。** 从 Intake 到复盘的主流程（第 216–246 行）以及阶段契约表（第 248–265 行）覆盖了普通 SDD 方案常遗漏的可行性、BRD、回归、发布、健康检查和复盘。

4. **安全方向正确。** 工具权限分级（第 408–428 行）、生产硬规则（第 451–460 行）和“生产默认人工审批”的原则（第 155–157 行）是必要底线。不可变制品、OIDC、staging 先行也符合生产交付的基本约束。

5. **避免过早平台化。** 先用 CLI/文件产物验证，再演进到 LangGraph 服务化和 Temporal 的路径（第 462–513 行）合理。Temporal 被放在多项目、长流程和 Worker 集群阶段，而非 MVP 的默认依赖。

## 4. P0：进入 M0 前必须解决

### P0-1：当前 MVP 同时承担完整研发与生产发布，无法形成可验证的最小闭环

**证据：** MVP 同时要求生成 BRD/PRD/技术方案/测试用例、写代码、执行多类测试、建立追踪、部署 staging，并在审批后部署生产（规划第 523–532 行）；M0–M5 虽有阶段划分，但 MVP 定义本身仍把 M5 的能力提前纳入了目标。

**风险：** 每一个子能力都需要独立的错误恢复、权限、测试夹具和环境集成。并行建设会使“工作流有没有价值”这个最早的问题得不到答案。

**必须修改：** 将 `v0.1` 明确收窄为：

```text
单一 Python/FastAPI 示例仓库
单个变更、单一 CLI、单人审批、单个 Git worktree
Intake -> PRD -> 技术方案 -> 任务 -> 代码/单测 -> 追踪报告
不含 Web UI、不含多项目、不含生产部署、不含自动回归选择
```

`staging` 放入 v0.2；生产发布、云身份和回滚放入 v0.3。这样仍然保留“需求到可验证代码”的核心价值，但可以在 2–3 个迭代内验证。

### P0-2：阶段状态机不可直接实现，无法保障恢复、审批与失败后的正确行为

**证据：** 规划的阶段生命周期只有 `PENDING → RUNNING → WAITING_REVIEW → APPROVED`，并将 `CHANGES_REQUESTED` 与 `FAILED` 画成支路（第 267–279 行）。其中 `APPROVED → FAILED` 的语义不明确，也没有 `REJECTED`、`BLOCKED`、`CANCELLED`、上游变更后的失效状态，以及副作用重试约束。

**风险：** Agent 调用、创建 worktree、提交分支、触发 CI、部署都是副作用。流程进程崩溃或审批结果重复送达时，可能重复创建分支、重复执行工具或错误推进阶段。

**必须修改：** 在实现前写出状态机、事件与幂等规则。建议最小模型为：

```text
PENDING -> RUNNING -> WAITING_REVIEW -> APPROVED
RUNNING -> BLOCKED | FAILED | CANCELLED
WAITING_REVIEW -> CHANGES_REQUESTED | REJECTED | EXPIRED
CHANGES_REQUESTED -> RUNNING
FAILED -> RETRYING -> RUNNING | ABORTED
APPROVED -> INVALIDATED  # 任一已引用的上游产物版本变化
```

每个 `StageRun` 至少持有：`attempt`、`input_artifact_hashes`、`idempotency_key`、`workspace_lock`、`tool_invocation_ids`、`policy_decision_id` 和 `last_event_id`。每个外部动作需先记录意图，再执行，再记录结果；恢复时按幂等键查询而不是盲目重跑。

### P0-3：安全规则目前是原则，尚未成为能拦截危险动作的策略系统

**证据：** 文档定义了 `READ_ONLY` 到 `DEPLOY_PRODUCTION` 的权限等级（第 408–428 行），也列出禁止宿主机危险命令、默认断网和禁止泄露密钥等规则（第 451–460 行），但没有定义哪个组件在每次工具调用前强制校验、策略输入是什么、拒绝如何审计。

**风险：** 如果权限只存在于 Prompt 或角色说明中，Agent 仍可能通过通用 Shell、Git 或网络工具绕开限制；Docker 容器本身也不等于安全沙箱。

**必须修改：** 将权限设计为工具网关的强制机制：

```text
ToolInvocation -> PolicyDecision -> Allow / Deny / RequireApproval
```

至少定义：命令白名单和参数校验、可写路径白名单、工作区挂载方式、默认网络隔离/出口代理、SecretReference（只传引用、不传明文）、短期服务身份、制品与 GitHub Action 的版本固定、审计日志脱敏与保留期。生产权限只允许 CI/CD 适配器持有，不能分发给 Agent 进程。

## 5. P1：应在 M1/M2 前补齐

### P1-1：没有定义产物的唯一事实来源、版本冲突与保留策略

**证据：** 组件表既定义“本地文件系统 + Git”又规划“PostgreSQL + S3/对象存储”（第 184–193 行）；目标仓库的 `.vaf/` 同时保存产物、追踪和 runs（第 291–321 行）；后文又建议大证据放对象存储（第 617–620 行）。

**建议：** 明确以下规则：

- Markdown/YAML 规格和审批结论：Git 是唯一事实来源。
- 运行事件、原始模型响应、测试日志：对象存储是事实来源；Git 仅保存哈希、URI 和脱敏摘要。
- PostgreSQL：只保存可重建的索引、运行投影与查询数据，不承担最终审计证据。
- `ArtifactVersion` 不可变；新版本替代旧版本，不能原地覆盖已审批版本。
- 对同一 Change 的并发运行实行 change 级锁；上游更新自动产生 `INVALIDATED` 事件。

### P1-2：追踪关系只有链路示意，缺少可查询的 Schema 和“覆盖”定义

**证据：** 规划要求 `REQ → US → AC → ADR/API → TASK → CODE_DIFF → TC/REG → EV → REL`（第 340–344 行），但没有说明关系类型、代码变更如何建立关联、哪些断链可以被接受、需求覆盖率怎么算。

**建议：** 引入不可变的 `TraceLink`：

```text
TraceLink {
  from_type, from_id,
  relation,            # satisfies / verifies / implements / derived_from
  to_type, to_id,
  evidence_ref,
  created_by, created_at, status
}
```

v0.1 的覆盖率只定义两项即可：

- `AC 覆盖率`：已批准 REQ 的每一个 AC 是否至少关联一个通过的 TC。
- `变更可解释率`：每个非格式化代码文件是否关联至少一个 TASK 与 REQ/AC。

不要在 v0.1 追求自动从 diff 推断语义关系；先要求 Agent 生成候选链接，再由 Schema 和评审门确认。

### P1-3：Lite / Standard / Enterprise Profile 缺少准入规则

**证据：** 风险表提出三种 Profile（第 600–611 行），决策清单也要求选择默认 Profile（第 613–623 行），但没有定义变更如何被分级。

**建议：** 将 Profile 变成 Policy Pack，并以客观条件自动上调。例如：

| 条件 | 最低 Profile | 不可跳过阶段 |
|---|---|---|
| 纯文案、测试、无行为变更 | Lite | 任务、验证、追踪 |
| 新接口、数据模型、外部 API | Standard | PRD、技术方案、测试设计、评审 |
| 数据迁移、鉴权/支付、生产配置、个人数据 | Enterprise | 可行性、威胁建模、完整回归、发布审批、回滚演练 |

### P1-4：缺少跨组件事件契约与 CI/CD 回调模型

**证据：** 架构图把工作流、沙箱、证据和 CI/CD 串起来（第 163–180 行），但没有定义任务何时创建、如何关联 run、回调重复时如何处理、测试/部署结果以什么结构进入 Evidence。

**建议：** 先定义 6 个事件：`StageStarted`、`ArtifactApproved`、`ToolCompleted`、`VerificationCompleted`、`DeploymentCompleted`、`StageInvalidated`。每个事件带 `run_id`、`change_id`、`stage_run_id`、`correlation_id`、`attempt` 和 `artifact_hash`。

### P1-5：评测、成本和可观测性没有验收指标

**证据：** 规划提出在两个真实小项目上比较返工率、测试通过率和需求覆盖率（第 646–647 行），也提出成本控制（第 609 行），但没有样本、基线、通过线或恢复演练。

**建议：** 在 M0 固定一个 Golden Change，至少增加以下验收：

- 所有产物通过 Schema 校验，且 TraceLink 无悬空引用。
- 进程中断后恢复，不重复执行同一幂等工具调用。
- 策略网关阻断未授权命令、非工作区写入和未批准部署。
- 每个 run 输出耗时、模型成本、工具耗时、重试次数和失败分类。
- 固定人工 Rubric 评估 PRD、技术方案和测试用例，避免只用“测试通过”评价质量。

## 6. P2：可在验证核心价值后处理

1. **BRD、需求分析和 PRD 的去重。** 目前三个阶段都可能重复描述目标、范围与用户场景（第 252–256 行）。可以先保留，但模板中应明确 BRD 负责业务价值，PRD 负责系统行为，需求分析只负责歧义/假设/问题清单。

2. **多 Agent 角色可以延后。** 角色表完整（规划第 381–393 行），但 v0.1 可以先使用一个“文档生成 Agent”和一个“验证/审阅 Agent”，角色差异通过模板和权限实现；等到有评测数据后再拆分角色。

3. **README 缺少项目导航。** [README.md](./README.md) 目前只说明愿景和规划链接（第 1–19 行）。开始 M0 后建议补充状态、范围、术语、最小命令、示例 Change 与评审报告入口。

## 7. 收敛后的 M0 设计建议

建议将原规划中的“第 1 步：定义领域对象和状态机”进一步收敛为以下可提交包：

```text
M0.1 领域模型
  Change, WorkflowRun, StageRun, ArtifactVersion, Approval,
  ToolInvocation, PolicyDecision, TraceLink

M0.2 状态机与事件
  transition table、失败/恢复规则、幂等键、change/workspace lock

M0.3 文件产物
  intake / prd / technical-design / implementation-plan / test-cases 的 Schema 与模板

M0.4 模拟执行器
  无模型输入，覆盖 approve / reject / retry / resume / invalidate

M0.5 端到端夹具
  一个 FastAPI 示例仓库、一条固定变更、一个 pytest 验证命令、一个 trace report
```

**M0 完成定义：** 不接入真实 LLM、不部署、不创建云资源；但能用固定夹具稳定证明状态机、审批、恢复、权限拒绝和追踪校验正确。通过后才进入真实模型和 Git worktree。

## 8. 对项目入口 README 的意见

README 保持简短是正确的：它准确说明了项目定位，并指向主规划。它不应重复 685 行的架构内容。

不过，下一次更新建议增加一个 10 行以内的“当前状态”区块：

```markdown
## 当前状态

- 阶段：架构细化 / M0 未开始
- 支持范围：待限定为单仓库 Python CLI 验证
- 当前文档：架构规划、架构评审报告
- 下一产物：领域模型、状态机、产物 Schema
```

这样新读者能立即知道项目是否已有可运行代码，也能区分愿景和已实现能力。

## 9. 最终意见

VAF 的核心选择是正确的：**用可审核、可追踪、可恢复的过程约束 Agent，而不是让 Agent 自己决定过程。**

下一步的优先级不应是接入更多模型、增加更多角色或创建 Web UI，而应依次完成：

1. 状态机与幂等副作用。
2. Policy Engine 的强制执行点。
3. 产物/追踪的 Schema 与唯一事实来源。
4. 一个不带部署的端到端 Golden Change。

完成这些后，VAF 将从一份完整的架构规划，变成一个可验证、可迭代的工程内核。
