# VAF 门禁设计

> 适用范围：VAF v0.1～v0.3 的阶段门、评审、评分、驳回和防幻觉机制。

## 1. 结论

图中的产品门、方案门、质量门、发布门可以保留，但不能只依赖一个总分。每个门必须同时满足：

```text
硬性条件全部通过
AND 可解释评分 > 90
AND 评审证据完整
```

**90.0 分不通过，必须严格大于 90 分。** 任何 P0 阻塞项、策略违规、悬空引用、验证失败、过期哈希或缺失证据都会直接否决，不允许用高分覆盖。

## 2. 门前评审链

门禁前必须存在自动评审；评审结果由 Gate Engine 计算，M0 不等待人工审批：

```text
Agent 生成候选产物
  -> 结构化解析与 Schema 校验
  -> 确定性规则评审
  -> 可选独立评分器评审
  -> Gate Engine 汇总分数、硬阻塞和证据
  -> 自动推进或自动驳回
  -> 继续循环或阻断
```

M0 已实现 Schema、对象引用和确定性 Gate 评审，并在 `review` 输出评分；`approve` 会再次对当前哈希执行 Gate 评审；`autopilot` 使用评分门禁自动推进。M0 尚未接入独立 LLM Critic，当前不把 Agent 自评当作评分证据。

## 3. 门禁定义

| 门禁 | 触发位置 | 主要检查 | M0 决策方式 |
|---|---|---|---|
| 可行性/需求门 | Intake、需求分析完成后 | 目标、范围、非目标、关键假设、可行性和风险 | 自动评分 |
| 产品门 | BRD/PRD 完成后 | REQ、AC、业务价值、边界、可验证性、需求追踪 | 自动评分 |
| 方案门 | 技术方案、测试设计完成后 | 架构、接口/数据、安全、异常、测试策略、兼容性 | 自动评分 |
| 质量门 | 代码、测试、Trace 和验证完成后 | 测试、静态扫描、变更范围、覆盖率、可复现证据 | 自动评分 |
| 发布门 | staging 验证完成后 | 制品完整性、健康检查、回滚、审批、变更窗口 | v0.2 后接入策略 |
| 运行复盘门 | 生产运行后 | SLO、事故、回滚、偏差、规则改进 | v0.3 后接入策略 |

可行性/需求门是当前图中最需要补强的一道门。它应发生在 BRD/PRD 生成前或初稿后，不能让 Agent 自己补齐关键业务事实。

## 4. 评分模型

评分必须按门禁使用不同 Rubric，不能所有阶段共用一个“AI 觉得不错”的分数。

### 产品门 100 分

| 维度 | 分值 |
|---|---:|
| 目标、用户和业务价值 | 15 |
| 范围、非目标和约束 | 15 |
| REQ 完整性和无歧义 | 25 |
| AC 可观察、可测试 | 20 |
| 风险、假设和可行性 | 15 |
| 需求到 AC 的追踪 | 10 |

### 方案门 100 分

| 维度 | 分值 |
|---|---:|
| 架构和边界 | 20 |
| 接口、数据和兼容性 | 20 |
| 安全、权限和隐私 | 20 |
| 异常、恢复和可观测性 | 15 |
| 测试策略和回归范围 | 15 |
| REQ/AC/TASK 追踪 | 10 |

### 质量门 100 分

| 维度 | 分值 |
|---|---:|
| 单测、集成测试和回归测试 | 30 |
| AC 覆盖率和 Trace 完整性 | 25 |
| 静态检查、安全扫描和依赖检查 | 20 |
| 代码变更范围符合任务 | 15 |
| 证据可复现、哈希一致 | 10 |

### 代码实现质量门的 M0 细分

`trace` 对代码候选使用独立的 100 分 Rubric。以下维度是当前已经落地的确定性评分，不依赖模型自评：

| 维度 | 分值 | 评分信号 |
|---|---:|---|
| 安全风险 | 20 | 高风险执行、反序列化、疑似硬编码凭据 |
| PRD 实现贴合度 | 20 | REQ/AC/TC/TASK 显式映射、覆盖率和验证错误 |
| 影响范围 | 15 | 变更文件必须在实施计划声明范围内 |
| 测试证据 | 20 | 当前 worktree 的白名单验证命令退出码为 0 |
| 架构与可维护性 | 10 | Python 语法和函数规模；过长函数扣分 |
| 最小改动与反过度设计 | 10 | 实际行数相对计划基线、变更文件数量 |
| 简洁度 | 5 | 注释/样板代码比例异常时扣分 |

P0 维度（安全、需求贴合、范围、测试和语法）失败会直接 `BLOCKED`，不能靠其他维度的高分覆盖。P1 维度（函数过长、改动过大、过度注释）会扣分，分数不严格大于 90 时进入 `NEEDS_CHANGES`。其中“架构优雅程度”在 M0 先用可解释代理指标约束，后续再引入独立评审器补充语义判断。

### 发布门 100 分

| 维度 | 分值 |
|---|---:|
| staging 验证和制品完整性 | 30 |
| 健康检查和 SLO 证据 | 25 |
| 回滚方案和演练结果 | 20 |
| 审批、权限和变更窗口 | 15 |
| 发布记录和审计完整性 | 10 |

## 5. 驳回和回退

门禁驳回不是简单地永远回到“上一步”，而是回到**最早受影响的阶段**：

| 问题类型 | 回退目标 |
|---|---|
| 当前文档结构或表达问题 | 当前阶段重新生成/修改 |
| PRD 的需求、范围、AC 发生变化 | 需求/产品门 |
| 技术方案或接口设计问题 | 方案门 |
| 测试不足、代码失败或 Trace 断链 | 测试设计或实现阶段 |
| staging/发布证据失败 | staging/发布候选阶段 |
| 生产健康检查失败 | 立即回滚；不自动回到需求阶段 |

普通评分不足时，每次驳回必须记录；P0 阻塞则记录 `GateBlocked`，等待新的可信输入，不能由 `autopilot` 盲目重试。两类结果都必须保留：

```yaml
decision: NEEDS_CHANGES
reason: "AC-003 不可验证"
correction_target_stage: test-cases
earliest_invalidated_stage: implementation
invalidated_artifact_hashes: [sha256:...]
```

上游产物产生新版本后，所有依赖旧哈希的下游产物必须标记 `INVALIDATED`，不能继续审批或生成代码。

## 6. 防幻觉机制

1. **来源约束：** Agent 只能读取已批准的 Artifact、仓库快照和显式规则；每个输入带路径、版本和哈希。
2. **结构化输出：** REQ、AC、TC、TASK、接口和风险必须有稳定 ID，禁止只输出自然语言结论。
3. **证据绑定：** 每个结论必须引用 Artifact、代码、测试或工具证据；没有证据只能标记 `UNVERIFIED`，不能计入通过分数。
4. **确定性校验：** Schema、引用存在性、哈希、编译、测试、静态扫描、路径范围和命令策略由代码检查，不交给 LLM 判断。
5. **独立评审：** 生成 Agent 不能同时担任唯一 Critic；后续至少使用独立上下文和独立提示的评审 Agent。
6. **不确定性阻塞：** 关键事实缺失、冲突或出现 `[BLOCKED]` 时进入 `BLOCKED`，等待新的可信输入，不允许 Agent 猜测。
7. **权限隔离：** 评审 Agent 不拥有写代码、部署或读取 Secret 的权限；工具动作继续经过 ToolGateway。
8. **反提示注入：** 仓库文档、代码注释和用户上传内容属于不可信数据，不能改变系统规则、门禁阈值或工具权限。
9. **自动推进边界：** M0 不等待人工审批，但发布门和生产权限仍由后续 Policy Pack 控制；自动评分不能授予尚未实现的生产权限。

## 7. M0 实现边界

当前 M0 已落地：

- `review` 输出确定性 Gate 评分和 findings。
- `approve` 重新校验当前 Artifact 哈希并执行 Gate；兼容旧 CLI，M0 无人工路径使用 `autopilot`。
- 阈值严格为 `score > 90`。
- P0 findings 直接进入 `BLOCKED`。
- `autopilot` 在分数通过时自动推进，不等待人工审批。
- `NEEDS_CHANGES` 会追加 `ArtifactChangesRequested`，默认将 `correction_target_stage` 设为当前产物阶段并回到该阶段重新生成。
- `BLOCKED` 会追加 `GateBlocked` 并停止自动推进，直到出现新的可信输入；安全、悬空引用、验证失败和关键事实缺失不能靠重试消除。
- FakeAgent 的 PRD、技术方案、测试用例和实施计划可通过当前基础 Rubric。

尚未落地：独立 LLM Critic、可配置 Rubric、人工角色权限、上游失效传播和跨阶段最早受影响目标计算。这些是下一阶段的实现项。
